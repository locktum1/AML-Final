<label for="{{ $for }}" class="text-[20px] mt-[10px] text-left">{{ $label }}</label>
<textarea name="{{ $for }}" id="{{ $for }}" rows="{{ $rows }}" class="text-black w-[{{ $width }}] m-w-[2px] text-[20px] shadow-sm rounded-[10px]" value="">
