<label for="{{ $for }}" class="text-[20px] mt-[10px] text-center">{{ $label }}</label>
<input type="{{ $type }}" name="{{ $for }}" id="{{ $for }}" class="text-black w-[{{ $width }}] m-w-[2px] text-[20px] shadow-sm rounded-[10px]" value="">
